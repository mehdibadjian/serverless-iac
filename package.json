{
  "name": "serverless-iac",
  "version": "1.0.0",
  "description": "A multi-cloud serverless project using Serverless Framework",
  "main": "index.js",
  "scripts": {
    "deploy:aws": "serverless deploy --config aws/serverless.yml",
    "deploy:azure": "serverless deploy --config azure/serverless.yml",
    "deploy:gcp": "serverless deploy --config gcp/serverless.yml"
  },
  "dependencies": {
    "serverless": "^3.0.0",
    "serverless-plugin-include-dependencies": "^5.0.0",
    "serverless-offline": "^8.0.0",
    "serverless-iam-roles-per-function": "^3.0.0"
  }
}
